services:
  client:
    image: prwt-client
    build:
      context: .
      dockerfile: docker/client/Dockerfile
    ports:
      - "8080:4200"
    volumes:
      - ./client:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
